# Stage 1 Handoff — `sv`

Generated by `scripts/l10n-handoff.mjs`. A snapshot, not a live artefact: the termbase that governs this work is [`editor-glossary.md`](./editor-glossary.md), and that is the file to edit if a decision here turns out wrong.

## Scope

- **111 of 312** strings present.
- **201 missing**, of which **62 are prose** (`.helper`).
- The prose is the harder half and is deliberately **last**: by the time you reach it you will have decided every term it refers to.
- Work the panel chrome and first-screen labels **first**. A language at 40% coverage looks entirely untranslated if the panel titles are English, and entirely translated if they are not.

## Mined Strings — Paste These First

8 strings taken from Home Assistant's own frontend, matched on **English text** and rejected where HA's value was itself untranslated. Check each one reads correctly in context before keeping it.

```json
  "view.option.list.label": "Lista",
  "view.option.column.label": "Kolumner",
  "entity.customised": "Konfigurerade",
  "start_date_mode.option.default.label": "I dag",
  "compact_days_to_show": "dagar",
  "compact_events_to_show": "Händelser",
  "today_indicator_style": "Stil",
  "refresh_interval": "Uppdateringsintervall",
```

| key | English | proposed | HA source key |
|---|---|---|---|
| `view.option.list.label` | List | **Lista** | `ui.components.media-browser.list` |
| `view.option.column.label` | Columns | **Kolumner** | `ui.panel.lovelace.editor.card.glance.columns` |
| `entity.customised` | Configured | **Konfigurerade** | `ui.panel.config.integrations.configured` |
| `start_date_mode.option.default.label` | Today | **I dag** | `ui.components.date-range-picker.ranges.today` |
| `compact_days_to_show` | Days | **dagar** | `ui.components.calendar.event.repeat.interval.daily` |
| `compact_events_to_show` | Events | **Händelser** | `ui.panel.config.developer-tools.tabs.events.title` |
| `today_indicator_style` | Style | **Stil** | `ui.panel.lovelace.editor.card.empty_state.style` |
| `refresh_interval` | Refresh Interval | **Uppdateringsintervall** | `ui.panel.lovelace.editor.card.generic.refresh_interval` |

## Your Terms

Decided once, for all nine languages. Use these; do not re-decide them.

| term | sense | use |
|---|---|---|
| time | the clock time printed on an event row | **Tid** |
| start date | the day the card's window begins | **Startdatum** |
| event | a calendar entry with a start and an end | **händelse** |
| calendar | a calendar entity supplying events | **Kalender** |
| entity | a Home Assistant entity | **Entitet** |
| weather | the weather integration and its forecast | **Väder** |
| location | the place an event happens | **Plats** |
| description | an event's description text | **Beskrivning** |
| label | the small text tag drawn beside an event | **Etikett** |
| all day | an event with no clock time | **Heldag** |
| today | the current day | **I dag** |
| layout | list layout versus column layout | **Layout** |
| columns | the vertical divisions of the column layout | **Kolumner** |
| list | the list layout | **Lista** |
| compact | compact mode | **Kompakt läge** |
| position | where something sits on the card | **Position** |
| separator | the rule drawn between days or weeks | **Avdelare** |
| day header | the date row heading each day's events | **you decide** |
| progress bar | the bar showing how far through an event we are | **Förloppsindikator** |
| countdown | the time remaining until an event starts | **Nedräkning** |
| week number | the ISO week number | **Veckonummer** |
| today indicator | the marker showing the current day or time | **Idag-indikator** |
| color | any colour setting | **Färg** |
| icon | an MDI icon | **Ikon** |
| background | a background colour or fill | **Bakgrund** |
| title | the card's title, and an event's title | **Titel** |
| width | a width in pixels | **Bredd** |
| height | a height in pixels | **Höjd** |
| size | a font size or icon size | **Storlek** |
| accent | the accent colour | **Accentfärg** |
| font | the typeface, as it appears in `Font Size` | **Font** |
| spacing | vertical gap between elements | **Avstånd** |
| opacity | the 0–100 opacity of a fill | **Opacitet** |
| show | the verbs on every toggle | **Visa** |
| hide | the verbs on every toggle | **Göm** |
| none | the "no such thing" option | **Ingen** |
| default | the "leave it alone" option | **Standard** |
| top | vertical alignment options | **Topp** |
| bottom | vertical alignment options | **Botten** |
| date | a calendar date | **Datum** |
| week | a calendar week | **Vecka** |
| weekday | a day of the week, Monday to Sunday | **Veckodag** |

### Undecided — You Own These

Nothing external arbitrates them: Home Assistant has no term, the card has no string, and no other language can supply one. Decide each, then **write it back into the glossary** so the next session finds it decided.

- **day header** — the date row heading each day's events

## Before You Start

1. Read [`editor-glossary.md`](./editor-glossary.md) §3 for this language's **casing rule**. It is one decision, not 201.
2. Fix the pre-existing strings first. `node scripts/check-i18n.mjs` lists every defect it can see in this file by name.
3. Placeholders (`{count}`, `{width}`), the em dash `—` and `≥` must survive verbatim; the checks fail the build if they do not. Quotation marks should become this language's own, not stay English.
4. Do not add an entry that is byte-identical to the English. If a word genuinely is the same, say so in `IDENTICAL_TO_ENGLISH_OK` in `scripts/check-i18n.mjs` rather than leaving it ambiguous.

