# Stage 1 Handoff — `de`

Generated by `scripts/l10n-handoff.mjs`. A snapshot, not a live artefact: the termbase that governs this work is [`editor-glossary.md`](./editor-glossary.md), and that is the file to edit if a decision here turns out wrong.

## Scope

- **134 of 312** strings present.
- **178 missing**, of which **50 are prose** (`.helper`).
- The prose is the harder half and is deliberately **last**: by the time you reach it you will have decided every term it refers to.
- Work the panel chrome and first-screen labels **first**. A language at 40% coverage looks entirely untranslated if the panel titles are English, and entirely translated if they are not.

## Mined Strings — Paste These First

6 strings taken from Home Assistant's own frontend, matched on **English text** and rejected where HA's value was itself untranslated. Check each one reads correctly in context before keeping it.

```json
  "view.option.list.label": "Liste",
  "view.option.column.label": "Spalten",
  "start_date_mode.option.default.label": "Heute",
  "compact_days_to_show": "Tage",
  "today_indicator_style": "Stil",
  "refresh_interval": "Aktualisierungsintervall",
```

| key | English | proposed | HA source key |
|---|---|---|---|
| `view.option.list.label` | List | **Liste** | `ui.components.media-browser.list` |
| `view.option.column.label` | Columns | **Spalten** | `ui.panel.lovelace.editor.card.glance.columns` |
| `start_date_mode.option.default.label` | Today | **Heute** | `ui.components.date-range-picker.ranges.today` |
| `compact_days_to_show` | Days | **Tage** | `ui.components.calendar.event.repeat.interval.daily` |
| `today_indicator_style` | Style | **Stil** | `ui.panel.lovelace.editor.card.empty_state.style` |
| `refresh_interval` | Refresh Interval | **Aktualisierungsintervall** | `ui.panel.lovelace.editor.card.generic.refresh_interval` |

### Rejected By The Miner

Home Assistant had a value for these and it was thrown away. Listed so the omission is visible rather than silent — if one looks salvageable, it still needs writing by hand.

| key | English | HA offered | why rejected |
|---|---|---|---|
| `compact_events_to_show` | Events | Ereignisse | contradicts a rejected term |

## Your Terms

Decided once, for all nine languages. Use these; do not re-decide them.

| term | sense | use |
|---|---|---|
| time | the clock time printed on an event row | **Uhrzeit** |
| start date | the day the card's window begins | **Startdatum** |
| event | a calendar entry with a start and an end | **Termin** |
| calendar | a calendar entity supplying events | **Kalender** |
| entity | a Home Assistant entity | **Entität** |
| weather | the weather integration and its forecast | **Wetter** |
| location | the place an event happens | **Ort** |
| description | an event's description text | **Beschreibung** |
| label | the small text tag drawn beside an event | **you decide** |
| all day | an event with no clock time | **Ganztägig** |
| today | the current day | **Heute** |
| layout | list layout versus column layout | **Layout** |
| columns | the vertical divisions of the column layout | **Spalten** |
| list | the list layout | **Liste** |
| compact | compact mode | **Kompaktmodus** |
| position | where something sits on the card | **Position** |
| separator | the rule drawn between days or weeks | **Trennlinie** |
| day header | the date row heading each day's events | **Tagesüberschrift** |
| progress bar | the bar showing how far through an event we are | **Fortschrittsbalken** |
| countdown | the time remaining until an event starts | **Countdown** |
| week number | the ISO week number | **Wochennummer** |
| today indicator | the marker showing the current day or time | **you decide** |
| color | any colour setting | **Farbe** |
| icon | an MDI icon | **Symbol** |
| background | a background colour or fill | **Hintergrund** |
| title | the card's title, and an event's title | **Titel** |
| width | a width in pixels | **Breite** |
| height | a height in pixels | **Höhe** |
| size | a font size or icon size | **Größe** |
| accent | the accent colour | **Akzentfarbe** |
| font | the typeface, as it appears in `Font Size` | **Schrift** |
| spacing | vertical gap between elements | **Abstand** |
| opacity | the 0–100 opacity of a fill | **Deckkraft** |
| show | the verbs on every toggle | **anzeigen** |
| hide | the verbs on every toggle | **ausblenden** |
| none | the "no such thing" option | **you decide** |
| default | the "leave it alone" option | **Standard** |
| top | vertical alignment options | **Oben** |
| bottom | vertical alignment options | **Unten** |
| date | a calendar date | **Datum** |
| week | a calendar week | **Woche** |
| weekday | a day of the week, Monday to Sunday | **Wochentag** |

### Undecided — You Own These

Nothing external arbitrates them: Home Assistant has no term, the card has no string, and no other language can supply one. Decide each, then **write it back into the glossary** so the next session finds it decided.

- **label** — the small text tag drawn beside an event
- **today indicator** — the marker showing the current day or time
- **none** — the "no such thing" option

## Before You Start

1. Read [`editor-glossary.md`](./editor-glossary.md) §3 for this language's **casing rule**. It is one decision, not 201.
2. Fix the pre-existing strings first. `node scripts/check-i18n.mjs` lists every defect it can see in this file by name.
3. Placeholders (`{count}`, `{width}`), the em dash `—` and `≥` must survive verbatim; the checks fail the build if they do not. Quotation marks should become this language's own, not stay English.
4. Do not add an entry that is byte-identical to the English. If a word genuinely is the same, say so in `IDENTICAL_TO_ENGLISH_OK` in `scripts/check-i18n.mjs` rather than leaving it ambiguous.

