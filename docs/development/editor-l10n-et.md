# Stage 1 Handoff — `et`

Generated by `scripts/l10n-handoff.mjs`. A snapshot, not a live artefact: the termbase that governs this work is [`editor-glossary.md`](./editor-glossary.md), and that is the file to edit if a decision here turns out wrong.

## Scope

- **111 of 312** strings present.
- **201 missing**, of which **62 are prose** (`.helper`).
- The prose is the harder half and is deliberately **last**: by the time you reach it you will have decided every term it refers to.
- Work the panel chrome and first-screen labels **first**. A language at 40% coverage looks entirely untranslated if the panel titles are English, and entirely translated if they are not.

## Mined Strings — Paste These First

9 strings taken from Home Assistant's own frontend, matched on **English text** and rejected where HA's value was itself untranslated. Check each one reads correctly in context before keeping it.

```json
  "view": "Paigutus",
  "view.option.list.label": "Loetelu",
  "view.option.column.label": "Veerud",
  "entity.customised": "Seadistatud",
  "start_date_mode.option.default.label": "Täna",
  "compact_days_to_show": "päeva",
  "compact_events_to_show": "Sündmused",
  "today_indicator_style": "Stiil",
  "refresh_interval": "Värskendamise intervall",
```

| key | English | proposed | HA source key |
|---|---|---|---|
| `view` | Layout | **Paigutus** | `ui.panel.lovelace.editor.edit_view.type` |
| `view.option.list.label` | List | **Loetelu** | `ui.components.media-browser.list` |
| `view.option.column.label` | Columns | **Veerud** | `ui.panel.lovelace.editor.card.glance.columns` |
| `entity.customised` | Configured | **Seadistatud** | `ui.panel.config.integrations.configured` |
| `start_date_mode.option.default.label` | Today | **Täna** | `ui.components.date-range-picker.ranges.today` |
| `compact_days_to_show` | Days | **päeva** | `ui.components.calendar.event.repeat.interval.daily` |
| `compact_events_to_show` | Events | **Sündmused** | `ui.panel.config.developer-tools.tabs.events.title` |
| `today_indicator_style` | Style | **Stiil** | `ui.panel.lovelace.editor.card.empty_state.style` |
| `refresh_interval` | Refresh Interval | **Värskendamise intervall** | `ui.panel.lovelace.editor.card.generic.refresh_interval` |

## Your Terms

Decided once, for all nine languages. Use these; do not re-decide them.

| term | sense | use |
|---|---|---|
| time | the clock time printed on an event row | **Kellaaeg** |
| start date | the day the card's window begins | **Alguskuupäev** |
| event | a calendar entry with a start and an end | **sündmus** |
| calendar | a calendar entity supplying events | **Kalender** |
| entity | a Home Assistant entity | **Olem** |
| weather | the weather integration and its forecast | **Ilm** |
| location | the place an event happens | **Asukoht** |
| description | an event's description text | **Kirjeldus** |
| label | the small text tag drawn beside an event | **Silt** |
| all day | an event with no clock time | **Terve päev** |
| today | the current day | **Täna** |
| layout | list layout versus column layout | **Paigutus** |
| columns | the vertical divisions of the column layout | **Veerud** |
| list | the list layout | **Loetelu** |
| compact | compact mode | **Kompaktrežiim** |
| position | where something sits on the card | **Asend** |
| separator | the rule drawn between days or weeks | **Eraldaja** |
| day header | the date row heading each day's events | **you decide** |
| progress bar | the bar showing how far through an event we are | **Edenemisriba** |
| countdown | the time remaining until an event starts | **Loendur** |
| week number | the ISO week number | **Nädalanumber** |
| today indicator | the marker showing the current day or time | **Tänase päeva indikaator** |
| color | any colour setting | **Värv** |
| icon | an MDI icon | **Ikoon** |
| background | a background colour or fill | **Taust** |
| title | the card's title, and an event's title | **Pealkiri** |
| width | a width in pixels | **Laius** |
| height | a height in pixels | **Kõrgus** |
| size | a font size or icon size | **Suurus** |
| accent | the accent colour | **Esiletõstmise värv** |
| font | the typeface, as it appears in `Font Size` | **Font** |
| spacing | vertical gap between elements | **Vahe** |
| opacity | the 0–100 opacity of a fill | **you decide** |
| show | the verbs on every toggle | **Näita** |
| hide | the verbs on every toggle | **Peida** |
| none | the "no such thing" option | **Puudub** |
| default | the "leave it alone" option | **Vaikimisi** |
| top | vertical alignment options | **Üleval** |
| bottom | vertical alignment options | **All** |
| date | a calendar date | **Kuupäev** |
| week | a calendar week | **Nädal** |
| weekday | a day of the week, Monday to Sunday | **Nädalapäev** |

### Undecided — You Own These

Nothing external arbitrates them: Home Assistant has no term, the card has no string, and no other language can supply one. Decide each, then **write it back into the glossary** so the next session finds it decided.

- **day header** — the date row heading each day's events
- **opacity** — the 0–100 opacity of a fill

## Before You Start

1. Read [`editor-glossary.md`](./editor-glossary.md) §3 for this language's **casing rule**. It is one decision, not 201.
2. Fix the pre-existing strings first. `node scripts/check-i18n.mjs` lists every defect it can see in this file by name.
3. Placeholders (`{count}`, `{width}`), the em dash `—` and `≥` must survive verbatim; the checks fail the build if they do not. Quotation marks should become this language's own, not stay English.
4. Do not add an entry that is byte-identical to the English. If a word genuinely is the same, say so in `IDENTICAL_TO_ENGLISH_OK` in `scripts/check-i18n.mjs` rather than leaving it ambiguous.

