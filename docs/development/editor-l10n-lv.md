# Stage 1 Handoff — `lv`

Generated by `scripts/l10n-handoff.mjs`. A snapshot, not a live artefact: the termbase that governs this work is [`editor-glossary.md`](./editor-glossary.md), and that is the file to edit if a decision here turns out wrong.

## Scope

- **111 of 312** strings present.
- **201 missing**, of which **62 are prose** (`.helper`).
- The prose is the harder half and is deliberately **last**: by the time you reach it you will have decided every term it refers to.
- Work the panel chrome and first-screen labels **first**. A language at 40% coverage looks entirely untranslated if the panel titles are English, and entirely translated if they are not.

## Mined Strings — Paste These First

5 strings taken from Home Assistant's own frontend, matched on **English text** and rejected where HA's value was itself untranslated. Check each one reads correctly in context before keeping it.

```json
  "view": "Izkārtojums",
  "view.option.column.label": "Kolonnas",
  "entity.customised": "Nokonfigurētās",
  "start_date_mode.option.default.label": "Šodien",
  "refresh_interval": "Atjaunošanās intervāls",
```

| key | English | proposed | HA source key |
|---|---|---|---|
| `view` | Layout | **Izkārtojums** | `glossary` |
| `view.option.column.label` | Columns | **Kolonnas** | `ui.panel.lovelace.editor.card.glance.columns` |
| `entity.customised` | Configured | **Nokonfigurētās** | `ui.panel.config.integrations.configured` |
| `start_date_mode.option.default.label` | Today | **Šodien** | `ui.components.date-range-picker.ranges.today` |
| `refresh_interval` | Refresh Interval | **Atjaunošanās intervāls** | `ui.panel.lovelace.editor.card.generic.refresh_interval` |

### Rejected By The Miner

Home Assistant had a value for these and it was thrown away. Listed so the omission is visible rather than silent — if one looks salvageable, it still needs writing by hand.

| key | English | HA offered | why rejected |
|---|---|---|---|
| `compact_days_to_show` | Days | d. | looks like an abbreviation or a fragment |

## Your Terms

Decided once, for all nine languages. Use these; do not re-decide them.

| term | sense | use |
|---|---|---|
| time | the clock time printed on an event row | **Laiks** |
| start date | the day the card's window begins | **Sākuma datums** |
| event | a calendar entry with a start and an end | **notikums** |
| calendar | a calendar entity supplying events | **Kalendārs** |
| entity | a Home Assistant entity | **Vienība** |
| weather | the weather integration and its forecast | **Laikapstākļi** |
| location | the place an event happens | **Atrašanās vieta** |
| description | an event's description text | **Apraksts** |
| label | the small text tag drawn beside an event | **Etiķete** |
| all day | an event with no clock time | **Visu dienu** |
| today | the current day | **Šodien** |
| layout | list layout versus column layout | **Izkārtojums** |
| columns | the vertical divisions of the column layout | **Kolonnas** |
| list | the list layout | **Saraksts** |
| compact | compact mode | **Kompaktais režīms** |
| position | where something sits on the card | **Pozīcija** |
| separator | the rule drawn between days or weeks | **Atdalītājs** |
| day header | the date row heading each day's events | **you decide** |
| progress bar | the bar showing how far through an event we are | **Progresa josla** |
| countdown | the time remaining until an event starts | **Atpakaļskaitīšana** |
| week number | the ISO week number | **Nedēļas numurs** |
| today indicator | the marker showing the current day or time | **Šodienas indikators** |
| color | any colour setting | **Krāsa** |
| icon | an MDI icon | **Ikona** |
| background | a background colour or fill | **Fons** |
| title | the card's title, and an event's title | **Virsraksts** |
| width | a width in pixels | **Platums** |
| height | a height in pixels | **Augstums** |
| size | a font size or icon size | **Izmērs** |
| accent | the accent colour | **Akcents** |
| font | the typeface, as it appears in `Font Size` | **Fonts** |
| spacing | vertical gap between elements | **Atstarpe** |
| opacity | the 0–100 opacity of a fill | **you decide** |
| show | the verbs on every toggle | **Rādīt** |
| hide | the verbs on every toggle | **Paslēpt** |
| none | the "no such thing" option | **you decide** |
| default | the "leave it alone" option | **Noklusējums** |
| top | vertical alignment options | **Augšā** |
| bottom | vertical alignment options | **Apakšā** |
| date | a calendar date | **Datums** |
| week | a calendar week | **Nedēļa** |
| weekday | a day of the week, Monday to Sunday | **Nedēļas diena** |

### Undecided — You Own These

Nothing external arbitrates them: Home Assistant has no term, the card has no string, and no other language can supply one. Decide each, then **write it back into the glossary** so the next session finds it decided.

- **day header** — the date row heading each day's events
- **opacity** — the 0–100 opacity of a fill
- **none** — the "no such thing" option

## Before You Start

1. Read [`editor-glossary.md`](./editor-glossary.md) §3 for this language's **casing rule**. It is one decision, not 201.
2. Fix the pre-existing strings first. `node scripts/check-i18n.mjs` lists every defect it can see in this file by name.
3. Placeholders (`{count}`, `{width}`), the em dash `—` and `≥` must survive verbatim; the checks fail the build if they do not. Quotation marks should become this language's own, not stay English.
4. Do not add an entry that is byte-identical to the English. If a word genuinely is the same, say so in `IDENTICAL_TO_ENGLISH_OK` in `scripts/check-i18n.mjs` rather than leaving it ambiguous.

