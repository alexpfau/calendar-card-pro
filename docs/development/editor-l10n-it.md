# Stage 1 Handoff — `it`

Generated by `scripts/l10n-handoff.mjs`. A snapshot, not a live artefact: the termbase that governs this work is [`editor-glossary.md`](./editor-glossary.md), and that is the file to edit if a decision here turns out wrong.

## Scope

- **111 of 312** strings present.
- **201 missing**, of which **62 are prose** (`.helper`).
- The prose is the harder half and is deliberately **last**: by the time you reach it you will have decided every term it refers to.
- Work the panel chrome and first-screen labels **first**. A language at 40% coverage looks entirely untranslated if the panel titles are English, and entirely translated if they are not.

## Mined Strings — Paste These First

9 strings taken from Home Assistant's own frontend, matched on **English text** and rejected where HA's value was itself untranslated. Check each one reads correctly in context before keeping it.

```json
  "view": "Layout",
  "view.option.list.label": "Lista",
  "view.option.column.label": "Colonne",
  "entity.customised": "Configurata",
  "start_date_mode.option.default.label": "Oggi",
  "compact_days_to_show": "giorni",
  "compact_events_to_show": "Eventi",
  "today_indicator_style": "Stile",
  "refresh_interval": "Intervallo di aggiornamento",
```

| key | English | proposed | HA source key |
|---|---|---|---|
| `view` | Layout | **Layout** | `glossary` |
| `view.option.list.label` | List | **Lista** | `ui.components.media-browser.list` |
| `view.option.column.label` | Columns | **Colonne** | `ui.panel.lovelace.editor.card.glance.columns` |
| `entity.customised` | Configured | **Configurata** | `ui.panel.config.integrations.configured` |
| `start_date_mode.option.default.label` | Today | **Oggi** | `ui.components.date-range-picker.ranges.today` |
| `compact_days_to_show` | Days | **giorni** | `ui.components.calendar.event.repeat.interval.daily` |
| `compact_events_to_show` | Events | **Eventi** | `ui.panel.config.developer-tools.tabs.events.title` |
| `today_indicator_style` | Style | **Stile** | `ui.panel.lovelace.editor.card.empty_state.style` |
| `refresh_interval` | Refresh Interval | **Intervallo di aggiornamento** | `ui.panel.lovelace.editor.card.generic.refresh_interval` |

## Your Terms

Decided once, for all nine languages. Use these; do not re-decide them.

| term | sense | use |
|---|---|---|
| time | the clock time printed on an event row | **Orario** |
| start date | the day the card's window begins | **Data di inizio** |
| event | a calendar entry with a start and an end | **evento** |
| calendar | a calendar entity supplying events | **Calendario** |
| entity | a Home Assistant entity | **Entità** |
| weather | the weather integration and its forecast | **Meteo** |
| location | the place an event happens | **Luogo** |
| description | an event's description text | **Descrizione** |
| label | the small text tag drawn beside an event | **Etichetta** |
| all day | an event with no clock time | **Tutto il giorno** |
| today | the current day | **Oggi** |
| layout | list layout versus column layout | **Layout** |
| columns | the vertical divisions of the column layout | **Colonne** |
| list | the list layout | **Lista** |
| compact | compact mode | **Modalità compatta** |
| position | where something sits on the card | **Posizione** |
| separator | the rule drawn between days or weeks | **Divisore** |
| day header | the date row heading each day's events | **you decide** |
| progress bar | the bar showing how far through an event we are | **Barra di progresso** |
| countdown | the time remaining until an event starts | **Conto alla rovescia** |
| week number | the ISO week number | **Numero settimana** |
| today indicator | the marker showing the current day or time | **Indicatore oggi** |
| color | any colour setting | **Colore** |
| icon | an MDI icon | **Icona** |
| background | a background colour or fill | **Sfondo** |
| title | the card's title, and an event's title | **Titolo** |
| width | a width in pixels | **Larghezza** |
| height | a height in pixels | **Altezza** |
| size | a font size or icon size | **Dimensione** |
| accent | the accent colour | **Accento** |
| font | the typeface, as it appears in `Font Size` | **Font** |
| spacing | vertical gap between elements | **Spaziatura** |
| opacity | the 0–100 opacity of a fill | **Opacità** |
| show | the verbs on every toggle | **Mostra** |
| hide | the verbs on every toggle | **Nascondi** |
| none | the "no such thing" option | **you decide** |
| default | the "leave it alone" option | **Predefinito** |
| top | vertical alignment options | **Alto** |
| bottom | vertical alignment options | **Basso** |
| date | a calendar date | **Data** |
| week | a calendar week | **Settimana** |
| weekday | a day of the week, Monday to Sunday | **Giorno della settimana** |

### Undecided — You Own These

Nothing external arbitrates them: Home Assistant has no term, the card has no string, and no other language can supply one. Decide each, then **write it back into the glossary** so the next session finds it decided.

- **day header** — the date row heading each day's events
- **none** — the "no such thing" option

## Before You Start

1. Read [`editor-glossary.md`](./editor-glossary.md) §3 for this language's **casing rule**. It is one decision, not 201.
2. Fix the pre-existing strings first. `node scripts/check-i18n.mjs` lists every defect it can see in this file by name.
3. Placeholders (`{count}`, `{width}`), the em dash `—` and `≥` must survive verbatim; the checks fail the build if they do not. Quotation marks should become this language's own, not stay English.
4. Do not add an entry that is byte-identical to the English. If a word genuinely is the same, say so in `IDENTICAL_TO_ENGLISH_OK` in `scripts/check-i18n.mjs` rather than leaving it ambiguous.

