# Stage 1 Handoff — `nb`

Generated by `scripts/l10n-handoff.mjs`. A snapshot, not a live artefact: the termbase that governs this work is [`editor-glossary.md`](./editor-glossary.md), and that is the file to edit if a decision here turns out wrong.

## Scope

- **111 of 312** strings present.
- **201 missing**, of which **62 are prose** (`.helper`).
- The prose is the harder half and is deliberately **last**: by the time you reach it you will have decided every term it refers to.
- Work the panel chrome and first-screen labels **first**. A language at 40% coverage looks entirely untranslated if the panel titles are English, and entirely translated if they are not.

## Mined Strings — Paste These First

7 strings taken from Home Assistant's own frontend, matched on **English text** and rejected where HA's value was itself untranslated. Check each one reads correctly in context before keeping it.

```json
  "view": "Layout",
  "view.option.list.label": "Liste",
  "view.option.column.label": "Kolonner",
  "entity.customised": "Konfigurert",
  "start_date_mode.option.default.label": "I dag",
  "compact_days_to_show": "dager",
  "refresh_interval": "Oppdateringsintervall",
```

| key                                    | English          | proposed                  | HA source key                                            |
| -------------------------------------- | ---------------- | ------------------------- | -------------------------------------------------------- |
| `view`                                 | Layout           | **Layout**                | `glossary`                                               |
| `view.option.list.label`               | List             | **Liste**                 | `ui.components.media-browser.list`                       |
| `view.option.column.label`             | Columns          | **Kolonner**              | `ui.panel.lovelace.editor.card.glance.columns`           |
| `entity.customised`                    | Configured       | **Konfigurert**           | `ui.panel.config.integrations.configured`                |
| `start_date_mode.option.default.label` | Today            | **I dag**                 | `ui.components.date-range-picker.ranges.today`           |
| `compact_days_to_show`                 | Days             | **dager**                 | `ui.components.calendar.event.repeat.interval.daily`     |
| `refresh_interval`                     | Refresh Interval | **Oppdateringsintervall** | `ui.panel.lovelace.editor.card.generic.refresh_interval` |

## Your Terms

Decided once, for all nine languages. Use these; do not re-decide them.

| term            | sense                                           | use                 |
| --------------- | ----------------------------------------------- | ------------------- |
| time            | the clock time printed on an event row          | **Tid**             |
| start date      | the day the card's window begins                | **Startdato**       |
| event           | a calendar entry with a start and an end        | **hendelse**        |
| calendar        | a calendar entity supplying events              | **Kalender**        |
| entity          | a Home Assistant entity                         | **Entitet**         |
| weather         | the weather integration and its forecast        | **Vær**             |
| location        | the place an event happens                      | **Sted**            |
| description     | an event's description text                     | **Beskrivelse**     |
| label           | the small text tag drawn beside an event        | **Etikett**         |
| all day         | an event with no clock time                     | **Hele dagen**      |
| today           | the current day                                 | **I dag**           |
| layout          | list layout versus column layout                | **Layout**          |
| columns         | the vertical divisions of the column layout     | **Kolonner**        |
| list            | the list layout                                 | **Liste**           |
| compact         | compact mode                                    | **Kompakt modus**   |
| position        | where something sits on the card                | **Posisjon**        |
| separator       | the rule drawn between days or weeks            | **Deler**           |
| day header      | the date row heading each day's events          | **you decide**      |
| progress bar    | the bar showing how far through an event we are | **Fremdriftslinje** |
| countdown       | the time remaining until an event starts        | **Nedtelling**      |
| week number     | the ISO week number                             | **Ukenummer**       |
| today indicator | the marker showing the current day or time      | **you decide**      |
| color           | any colour setting                              | **Farge**           |
| icon            | an MDI icon                                     | **Ikon**            |
| background      | a background colour or fill                     | **Bakgrunn**        |
| title           | the card's title, and an event's title          | **Tittel**          |
| width           | a width in pixels                               | **Bredde**          |
| height          | a height in pixels                              | **Høyde**           |
| size            | a font size or icon size                        | **Størrelse**       |
| accent          | the accent colour                               | **Aksentfarge**     |
| font            | the typeface, as it appears in `Font Size`      | **Skrift**          |
| spacing         | vertical gap between elements                   | **Avstand**         |
| opacity         | the 0–100 opacity of a fill                     | **you decide**      |
| show            | the verbs on every toggle                       | **Vis**             |
| hide            | the verbs on every toggle                       | **Skjul**           |
| none            | the "no such thing" option                      | **Ingen**           |
| default         | the "leave it alone" option                     | **Standard**        |
| top             | vertical alignment options                      | **Topp**            |
| bottom          | vertical alignment options                      | **Bunn**            |
| date            | a calendar date                                 | **Dato**            |
| week            | a calendar week                                 | **Uke**             |
| weekday         | a day of the week, Monday to Sunday             | **Ukedag**          |

### Undecided — You Own These

Nothing external arbitrates them: Home Assistant has no term, the card has no string, and no other language can supply one. Decide each, then **write it back into the glossary** so the next session finds it decided.

- **day header** — the date row heading each day's events
- **today indicator** — the marker showing the current day or time
- **opacity** — the 0–100 opacity of a fill

## Before You Start

1. Read [`editor-glossary.md`](./editor-glossary.md) §3 for this language's **casing rule**. It is one decision, not 201.
2. Fix the pre-existing strings first. `node scripts/check-i18n.mjs` lists every defect it can see in this file by name.
3. Placeholders (`{count}`, `{width}`), the em dash `—` and `≥` must survive verbatim; the checks fail the build if they do not. Quotation marks should become this language's own, not stay English.
4. Do not add an entry that is byte-identical to the English. If a word genuinely is the same, say so in `IDENTICAL_TO_ENGLISH_OK` in `scripts/check-i18n.mjs` rather than leaving it ambiguous.
