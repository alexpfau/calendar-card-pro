# Stage 1 Handoff — `pl`

Generated by `scripts/l10n-handoff.mjs`. A snapshot, not a live artefact: the termbase that governs this work is [`editor-glossary.md`](./editor-glossary.md), and that is the file to edit if a decision here turns out wrong.

## Scope

- **111 of 312** strings present.
- **201 missing**, of which **62 are prose** (`.helper`).
- The prose is the harder half and is deliberately **last**: by the time you reach it you will have decided every term it refers to.
- Work the panel chrome and first-screen labels **first**. A language at 40% coverage looks entirely untranslated if the panel titles are English, and entirely translated if they are not.

## Mined Strings — Paste These First

8 strings taken from Home Assistant's own frontend, matched on **English text** and rejected where HA's value was itself untranslated. Check each one reads correctly in context before keeping it.

```json
  "view": "Układ",
  "view.option.list.label": "Lista",
  "view.option.column.label": "Kolumny",
  "entity.customised": "Skonfigurowane",
  "start_date_mode.option.default.label": "Dzisiaj",
  "compact_days_to_show": "dni",
  "today_indicator_style": "Styl",
  "refresh_interval": "Częstotliwość odświeżania",
```

| key                                    | English          | proposed                      | HA source key                                            |
| -------------------------------------- | ---------------- | ----------------------------- | -------------------------------------------------------- |
| `view`                                 | Layout           | **Układ**                     | `ui.panel.lovelace.editor.edit_view.type`                |
| `view.option.list.label`               | List             | **Lista**                     | `ui.components.media-browser.list`                       |
| `view.option.column.label`             | Columns          | **Kolumny**                   | `ui.panel.lovelace.editor.card.glance.columns`           |
| `entity.customised`                    | Configured       | **Skonfigurowane**            | `ui.panel.config.integrations.configured`                |
| `start_date_mode.option.default.label` | Today            | **Dzisiaj**                   | `glossary`                                               |
| `compact_days_to_show`                 | Days             | **dni**                       | `ui.components.calendar.event.repeat.interval.daily`     |
| `today_indicator_style`                | Style            | **Styl**                      | `ui.panel.lovelace.editor.card.empty_state.style`        |
| `refresh_interval`                     | Refresh Interval | **Częstotliwość odświeżania** | `ui.panel.lovelace.editor.card.generic.refresh_interval` |

## Your Terms

Decided once, for all nine languages. Use these; do not re-decide them.

| term            | sense                                           | use                  |
| --------------- | ----------------------------------------------- | -------------------- |
| time            | the clock time printed on an event row          | **Czas**             |
| start date      | the day the card's window begins                | **Data początkowa**  |
| event           | a calendar entry with a start and an end        | **wydarzenie**       |
| calendar        | a calendar entity supplying events              | **Kalendarz**        |
| entity          | a Home Assistant entity                         | **Encja**            |
| weather         | the weather integration and its forecast        | **Pogoda**           |
| location        | the place an event happens                      | **Lokalizacja**      |
| description     | an event's description text                     | **Opis**             |
| label           | the small text tag drawn beside an event        | **Etykieta**         |
| all day         | an event with no clock time                     | **Cały dzień**       |
| today           | the current day                                 | **Dzisiaj**          |
| layout          | list layout versus column layout                | **Układ**            |
| columns         | the vertical divisions of the column layout     | **Kolumny**          |
| list            | the list layout                                 | **Lista**            |
| compact         | compact mode                                    | **Tryb kompaktowy**  |
| position        | where something sits on the card                | **Pozycja**          |
| separator       | the rule drawn between days or weeks            | **Dzielnik**         |
| day header      | the date row heading each day's events          | **you decide**       |
| progress bar    | the bar showing how far through an event we are | **Pasek postępu**    |
| countdown       | the time remaining until an event starts        | **Odliczanie**       |
| week number     | the ISO week number                             | **Numer tygodnia**   |
| today indicator | the marker showing the current day or time      | **Wskaźnik dzisiaj** |
| color           | any colour setting                              | **Kolor**            |
| icon            | an MDI icon                                     | **Ikona**            |
| background      | a background colour or fill                     | **Tło**              |
| title           | the card's title, and an event's title          | **Tytuł**            |
| width           | a width in pixels                               | **Szerokość**        |
| height          | a height in pixels                              | **Wysokość**         |
| size            | a font size or icon size                        | **Rozmiar**          |
| accent          | the accent colour                               | **Akcent**           |
| font            | the typeface, as it appears in `Font Size`      | **Czcionka**         |
| spacing         | vertical gap between elements                   | **Odstęp**           |
| opacity         | the 0–100 opacity of a fill                     | **you decide**       |
| show            | the verbs on every toggle                       | **Pokaż**            |
| hide            | the verbs on every toggle                       | **Ukryj**            |
| none            | the "no such thing" option                      | **Brak**             |
| default         | the "leave it alone" option                     | **Domyślny**         |
| top             | vertical alignment options                      | **Góra**             |
| bottom          | vertical alignment options                      | **Dół**              |
| date            | a calendar date                                 | **Data**             |
| week            | a calendar week                                 | **Tydzień**          |
| weekday         | a day of the week, Monday to Sunday             | **Dzień tygodnia**   |

### Undecided — You Own These

Nothing external arbitrates them: Home Assistant has no term, the card has no string, and no other language can supply one. Decide each, then **write it back into the glossary** so the next session finds it decided.

- **day header** — the date row heading each day's events
- **opacity** — the 0–100 opacity of a fill

## Before You Start

1. Read [`editor-glossary.md`](./editor-glossary.md) §3 for this language's **casing rule**. It is one decision, not 201.
2. Fix the pre-existing strings first. `node scripts/check-i18n.mjs` lists every defect it can see in this file by name.
3. Placeholders (`{count}`, `{width}`), the em dash `—` and `≥` must survive verbatim; the checks fail the build if they do not. Quotation marks should become this language's own, not stay English.
4. Do not add an entry that is byte-identical to the English. If a word genuinely is the same, say so in `IDENTICAL_TO_ENGLISH_OK` in `scripts/check-i18n.mjs` rather than leaving it ambiguous.
