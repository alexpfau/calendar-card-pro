name: Build & Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      # Read the Node version from .nvmrc, exactly as ci.yml does. This job is the one
      # that runs `npm ci` against the lockfile and publishes what every user downloads,
      # so it is the last place that should run a runtime nothing else tested on. It
      # hardcoded Node 24 while .nvmrc pinned 22, which is the same npm-major drift
      # recorded at ci.yml's pinning check (#404): npm 11 tolerates a package-lock.json
      # that npm 10 rejects, so the mismatch stays invisible until the install it breaks
      # is the one building the release.
      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install dependencies
        run: npm ci

      # A tag that disagrees with package.json ships a bundle whose embedded version
      # string does not match the release it is attached to. Cheap to check, painful
      # to discover after publishing.
      - name: Verify tag matches package.json
        run: |
          PKG_VERSION="$(node -p "require('./package.json').version")"
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          if [ "$PKG_VERSION" != "$TAG_VERSION" ]; then
            echo "::error::Tag ${GITHUB_REF_NAME} does not match package.json version ${PKG_VERSION}."
            exit 1
          fi
          echo "Version check passed: ${PKG_VERSION}"

      # Fail before building if the curated notes are missing, so the problem surfaces
      # in seconds rather than after a full lint + build cycle.
      - name: Extract release notes
        run: |
          node scripts/extract-release-notes.mjs "${GITHUB_REF_NAME}" > release-body.md
          echo "Release body: $(wc -l < release-body.md) lines"

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npx tsc --noEmit

      - name: Build
        run: npm run build

      # The release is the only thing users ever download, and this is the build that
      # produces it — so the multi-file assertions run here too, not only on pull
      # requests. A file importing another, a card that names the wrong editor, an
      # `import.meta` lowered to `{}` by esbuild, or a stale file left over from an
      # earlier build would otherwise be published and then downloaded by every user.
      - name: Verify bundle integrity
        run: npm run check:bundle

      # For people who install by copying files rather than through HACS. The card is not
      # a single file — it is the card plus a separate editor, and the card fetches the
      # editor by URL when someone opens it — so copying just the .js named in hacs.json
      # yields a card that renders but cannot be configured. The zip is flat, matching the
      # layout HACS itself writes into <config>/www/community/calendar-card-pro/.
      #
      # HACS downloads it like any other asset but never registers it: `filename` in
      # hacs.json selects the Lovelace resource, and it names the .js.
      #
      # The .gz siblings are what make a manual install as cheap over the wire as a HACS
      # one. Home Assistant's static handler serves a pre-compressed file when it finds
      # one beside the original and does not compress on the fly, which is why a
      # HACS-installed card arrives gzipped and a hand-copied one does not — HACS writes
      # a .gz for every asset it downloads, and extracting a zip writes none. Verified
      # against a running instance from /local/, not only /hacsfiles/: with a .gz present
      # the response carries Content-Encoding: gzip and the compressed length. Without
      # them a manual installer pays 188 KB for the card and 293 KB for the editor
      # instead of 57 KB and 83 KB.
      #
      # Built in a staging directory rather than in dist/, so `check:bundle` above keeps
      # asserting exactly what rollup emitted and the `dist/*.js` asset glob below cannot
      # pick up a compressed copy.
      - name: Package dist for manual installation
        run: |
          mkdir -p pkg
          cp dist/*.js pkg/
          gzip -9 -k pkg/*.js
          zip -q -j calendar-card-pro.zip pkg/*
          unzip -l calendar-card-pro.zip

      # dist/*.js deliberately, not dist/* — the glob does not match *.js.map, so the
      # sourcemap decision (still off, see rollup.config.mjs) is unaffected by this. It
      # matches exactly two files, and `check:bundle` above has already asserted that.
      - name: Release
        uses: softprops/action-gh-release@v3
        with:
          draft: true
          body_path: release-body.md
          files: |
            dist/*.js
            calendar-card-pro.zip
